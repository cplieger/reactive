name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    uses: cplieger/ci/.github/workflows/ts-ci.yaml@f024f691d9c2fcbaff3c74231a4cb25126630056 # v1
    with:
      working-directory: "."
