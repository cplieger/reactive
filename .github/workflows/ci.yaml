name: CI
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    uses: cplieger/ci/.github/workflows/ts-ci.yaml@98c2f685fa19abbbc43735b1108bfd53d30e65e0 # v1
    with:
      working-directory: "."
